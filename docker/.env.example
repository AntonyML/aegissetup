# © Antony Monge López — Costa Rica — Céd. 604700548
# Copiar a .env y ajustar. Collation debe igualar prod (Modern_Spanish_CI_AS).
SA_PASSWORD=Sidc*2026*Dev
SQL_PORT=14333
SQL_COLLATION=Modern_Spanish_CI_AS
# Usuario SQL solo para dev (prod usa Windows Auth, Docker Linux no puede).
# El DSN de dev usara este login en vez de Trusted_Connection.
SQL_APP_LOGIN=sidc_dev
SQL_APP_PASSWORD=Sidc*2026*App
